/** @type {import('next').NextConfig} */
const nextConfig = {
  // Is line ko add karein taake Serveo ka link allow ho sake
  allowedDevOrigins: ["*.serveousercontent.com"],
  allowedDevOrigins: ["*.shares.zrok.io"],
};

export default nextConfig;
